# Журнал потолков и обходов (THEORY companion)
### AIra · 2026-08 · живой документ: каждый потолок имеет статус и выбранный обход

> Формат: формулировка в пределе → почему лобовой путь не работает → обходы (со сравнением) → выбранный маршрут. Статусы: 🟥 открыт · 🟨 есть обход-гипотеза · 🟩 обход выбран и обоснован.

---

## C1. Транспорт ошибки в локальное обучение 🟨

**Предел.** Полный вектор ошибки δ ∈ ℝ^d нельзя доставить в каждый синапс изелированным k-канальным бродкастом: отклонение скетча ‖δ̂ − δ‖ ~ √(d/k)·‖δ‖ (см. THEORY §5). Лобовой ответ «увеличим k» упирается в анатомию: каналов нейромодуляции в мозге ~ десятки, и наши k ограничены бюджетом вещания.

**Обходы:**
1. *Усреднение по времени* (встроено в eligibility): S ≥ (d/k)(1−γ²)/γ² событий на уклон с выравниванием γ. При k=10³, γ=0.3 → ~10⁴ событий (~секунды) — приемлемо, но прогнозирует «пророчество медленного обучения» при малом k. Замечаем парадокс ожидания: чем ниже канальность, тем медленнее *первое* приобретение, но не его устойчивость.
2. *Иерархия бродкаста*: блоки популяций со своими подканалами; дисперсия на синапс ~ размеру блока m/k_b, не глоб d/k. Топология вещания становится наследуемой структурой (обход 3 в THEORY §5 + Zador-приоры). Цена: нужен дизайн «схемы вещания» — вопрос переносится в эволюционный слой (H-08).
3. *Эмерджентное выравнивание (DFA-эффект)*: прямые пути поворачиваются в подпространство фиксированной обратной проекции за счёт самой динамики обучения. Бесплатно, но недетерминированно — лечить как бонус, не как основу.

4. *Зонная σ-δ шина + гейт (цикл 2, THEORY §12):* дальний транспорт ошибки заменяется схемой мозга — внутри зон остатки локальны, между зонами передаются только события-остатки выше θ_bus, маршрутизация — обучаемыми воротами (таламус-аналог, H-17), дальний кредит сознательно ранговый (скетч) по скелету-шине. Бюджет полосы падает от зоны к магистрали.

**Выбрано:** комбинация 1+2, и 4 как структурный каркас для 2; проверка: H-09 (измерение cos по слоям и по времени при k ∈ {1, 10², 10³}), H-16 (сжатие трафика шиной), H-17 (гейт).

---

## C2. Временное кредитование за пределами окна следа 🟨

**Предел.** Eligibility-след с постоянной λ имеет горизонт ~1/(1−λ); кредит на интервалах ≫ горизонта теряется (ошибка усечения ~ λ^K).

**Обходы:**
1. *Банк шкал* (multi-timescale traces): набор следов {λ_m} до шагов консолидации — аппроксимирует бесконечный горизонт суммой экспонент ≈ степенному ядру (известная техника time-scale banks).
2. *Иерархическое время*: медленные следы живут на уровне блоков/модулей (свернуть популяции — получать кредит модуля), выбор явно задаёт дизайн структуры (как мозг: локальная пластичность мс + кортикальная консолидация часы + сон дни).
3. *Сон как внеполосной кредит*: mimo-временные последствия переносятся фазами консолидации, не следами (реплей отвечает за «длинные» связи событие→награда).

**Выбрано:** 1 (базовое) + 3 (структурное); проверка в P2/P6: задачи с задержанным вознаграждением на 10²–10³ шагов.

---

## C3. Рост ошибки пороговых обновлений по глубине 🟨

**Предел.** Лемма §3 даёт θ/(1−λ) на звено; наивная композиция — e ≤ Lθ/(1−λ): сколь угодно экономная пропускная схема «тратит» запас ошибки на глубину.

**Обходы:**
1. θ_l ~ 1/L-шкалирование порогов по слоям (цена: больше событий в глубоких слоях — измерим).
2. *Блочная синхронизация*: при «опасном» накоплении ŝ ошибки — сбрасочное событие на всю глубину (ридикул синхронизации сигма-дельта в ADC-практике: периодический точный пересчёт). Частота сброса — новая ручка энергии.
3. *Самоконтроль дрейфа*: апикальная каморка оценивает локальный остаток и инициирует обновление своего блока — децентрализованный вариант п.2.

**Выбрано:** 2+3; метрика: дрейф KL на длинной генерации при глубине 12–24 (H-02 расширение, вести в P2).

**Биологический прецедент (цикл 2):** медленные ритмы (тета 4–8 Гц) регулярно разрывают межзонную синхронию и дают «системный сброс» выборки (THEORY §12 F4в) — у мозга частота сбросов встроена в субстрат, а не добавлена; для нас это аргумент, что ручка «частота сброса» (п.2) имеет живой прототип и вариационный вывод по (θ, λ, L) — задача N5.

---

## C4. Эмуляционный налог плотного железа 🟩 (обоснован обход)

**Предел.** Нативные ценности экономии (событие, локальность) теряются на плотных тензорах: потенциальный налог ~10²–10³ (THEORY §10).

**Выбранный обход:** блочная событийность с g*, предсказанной формулой оптимума g* ≈ ln(1/α)/ρ (при ρ=10⁻² — порядка сотен; согласуется с SIMD-шириной 32–128 и типичной блочностью GPU-ядер). Риал: сохранение ~90% экономии при плотной арифметике внутри блока. Проверка: H-12 (карта ρ × g → реальные ватты).

---

## C5. Recall длинного контекста без KV-шины 🟥

**Предел.** Рекуррентное состояние SSM-класса имеет ёмкость ~размеру состояния; точное воспроизведение редкого далёкого факта (needle) без адресной памяти не гарантируется никаким сжатием.

**Обходы-кандидаты (нужна теория ёмкости):**
1. Гибрид: локальное короткое внимание (окно) + рекуррентное ядро + *эпизодическая HDC-память* (H-05) как адресный стор редких фактов — декомпозиция «гладкий контекст vs точные факты».
2. Ассоциативные слои (modern Hopfield) как блочная адресуемая память внутри стека.
3. Теоретическая часть: вывести границу ёмкости состояния (информационно: I(факт; состояние) ≤ H(состояние) бит) и формальный критерий, когда нужен внешний стор.

**Статус:** открыт в теоретической части (N3: граница ёмкости состояния), но **практический обход №1/2 подтверждён измерением**:
- EXP-05 (2026-08-05) — HDC+DAM стор с одно-событийной записью на синтетическом мире фактов: acc 1.000 при 1,28·10⁴ эпизодах, ×10³–10⁴ дешевле KV-скана по прокси, гейт фамильярности бесплатен (EXP05_REPORT.md, BRAIN_CUES.md §4).
- EXP-06 (2026-08-06) — **перенос на живой коррелированный текст**: 698 предложений «Кавказского пленника» + фон ×24 из «Войны и мира» (16 698 эпизодов): cloze hit@1 = 0.998, забывания нет (разрыв по возрасту 0.033), ручная сюжетная батарея 9/12 top3, гейт отсекает чужие 0.946 (порог 0.95 — погранично; к улучшению скор margin-статистикой). Метод-находка: sign-на-sign скоринг при idf-весах инвертирует ранжирование → запрос не квантуется (1-бит хранение сохранено). Это *практический* ответ на вопрос «как вспоминать без ленты», не закрывающий теоретическую задачу N3 (границы ёмкости рекуррентного состояния и критерий, когда стор обязателен). Следующие шаги по линии C5: встройка стора в LM-блок M3 (H-21🔬) и шкалирование на 10–100 Мток (нужен индекс/партиционирование, идея грубого скелета FF-16).

---

## C6. Глубина локальных правил на LLM-шкале 🟥 (переформулирован в цикле 2 как трилемма)

> **Обновление 2026-08-06 (EXP-07, механизм).** При росте ‖W‖ дискретная карта Эйлера релаксации
> попадает в **предельный цикл**: остаток залипает на 3.5–6.0 при любом T (до 64), cos-выравнивание
> по слоям уходит в минус; диагональный Якоби НЕ лечит (первый отрицательный результат спецификации
> из EXP-04). Лечится солвером «diag-предобусловливатель зоны × спектральный BB1-шаг на зону ×
> сторож монотонности энергии»: остаток 0.24 к T=32, min-cos 0.86 при T=8. Бонусом открыт
> **событийный солвер** (N2-2а): в цикле/после сходимости 71–88% координат имеют ничтожный
> градиент — покоординатный сон (freeze) снимает 84–94% работы без потери cos. Остаточный зазор
> PC↔BP при обучении (+7–10% к 800 шагам при любом солвере, сходится к +3.5% к 1500 по EXP-04) —
> это уже налог недорелаксации-смещения, а не жёсткости; двухфазный оценщик против него пока
> не работает (остаток доминирует). → EXP07_REPORT.md.
>
> **Обновление 2026-08-06 (EXP-10, первая измеренная производная риска).** Запущена серия
> «лестница масштаба» (правило S): зазор PC↔BP против ёмкости блока {117k, 352k, 800k} измерен
> в трёх рецептах. Итог: наклон +9–12 п.п. на 0.83 декады — риск C6 получил **число и цену**,
> а не только структуру. Разложение: переносимость замороженных гиперпараметров ✗ (нужны законы
> µTransfer-подобия — H-28); поздняя регрессия от β=const вылечена β-затуханием (−6.3 п.п. на
> 800k); остаточный рост — жёсткость релаксации больших зон → повышает приоритет долга M2.1
> (resid < 0.01) и делает редкие BP-якоря оформленной страховкой M4, а не абстракцией.
> Цена измерения производной риска — 15 минут на стойке M1. → EXP10_REPORT.md.
>
> **Обновление 2026-08-06 (EXP-11, перенос солвера и форма страховки).** (а) Найден первый
> закон переноса солвера: **глубина релаксации связана с β**, T(β) 32→64 по затуханию β 1.0→0.1
> (−2.8 п.п. на 800k; механизм: глубина при сильном β вбирает β-смещение точнее — мелкий
> недорелакс регуляризует как SGD-шум). (б) C6-страховка «редкие BP-якоря» получила два
> измеренных отрицательных результата: наивная подмена шага разрушительна (+65% — пила шкал в
> общем адаптивном оптимизаторе; тот же класс болезни, что σ-δ разнос θ=0.15, EXP-02), а
> RMS-согласованная форма нейтральна при бюджете 5% → страховка берётся в послойной форме
> (H-29c). Наклон лестницы после лекарств +6.2 п.п./0.83 дек — M2.1 (сильный Π, resid<0.01)
> официально блокер M4. → EXP11_REPORT.md.
>
> **Обновление 2026-08-06 (EXP-12, M2.1 переосмыслен).** Ускорение Андерсона встроено
> в релаксацию зоны (KKT-коэффициенты, сторож энергии) и проверено четырьмя режимами:
> при β=1 AA — яд (послойный cos до −0.03 — «не трогать шум β=1-фазы» подтверждено
> независимым измерением), при β=0.1 жёсткости нет и без него (resid 512
> 0.028–0.031 — **«512-жёсткая» была болезнью фазы β=1, не зоны**). На дистанции
> AA-вентиль нейтрален (96 +6.0% vs +5.06%; 512 +11.07% vs +11.3%) → H-30 ✗ для
> обучения, ✓ в арсенал точных режимов. Линия M2.1 «сильный Π» снята: корректный
> рычаг — закон K1 (T-β) уже адресует корень; остаточная болезнь переформулирована
> честной асимптотой @2400 (минимум +11.2%@1800 → дрейф +7.8% к +19.2% финала):
> **поздний дрейф большой зоны на фазе малого β при константном lr** → H-31
> (закон K2, финал lr-расписания, EXP-13). Наклон @1200 неизменен:
> +6.2 п.п./0.83 дек. → EXP12_REPORT.md.
>
> **Обновление 2026-08-06 (EXP-13, H-31 ✓ — закон K2 принят).** Поздний дрейф 512
> @2400 устранён полностью финалом lr ×0.3 последней трети: step-форма — **зазор
> +5.64% против BP@2400** (было +19.2%), дрейф хвоста +0.00%; косинусная — +6.10%,
> также устойчива. Качественный сдвиг: с K2 длинная дистанция *лучше* короткой
> (+5.64% @2400 < +11.3% @1200) — хвост обучения из источника болезни стал
> источником точности. Спецификация **M2/M3 v3.2 = β 1→0.1 · T(β) 32→64 (K1) ·
> lr ×0.3 последней трети (K2, step)** — оба закона по фазе, не по размеру
> (переносимы по построению). Очередь: длинная точка 96 @2400 (пересчёт наклона
> — прогноз +2 п.п./0.83 дек, впервые в воротах H-27), валидация K1+K2 на 2048-зоне.
> → EXP13_REPORT.md.
>
> **Обновление 2026-08-06 (EXP-13, длинная пара — наклон в воротах).** В тот же заход
> снята вторая точка лестницы той же спецификации: 96 @2400 → 1.1255 против BP
> 1.1071 (**+1.66%**, дрейф +0.00%, работа 0.132), 512 → +5.64%. **Наклон +3.98
> п.п./0.83 дек — впервые в воротах H-27 (≤+5)** после серии +11.3→+9.0→+6.2 п.п.
> По абсолюту на корпусе 8.1M младшая зона сильнее и у BP (ёмкость 512
> недоиспользуется — зазоры не портятся, но сборку S0-LM вести на диапазоне
> 96–256). Остаток C6-работы до M4: контроль полной лестницы @2400, валидация
> K1+K2 на 2048-зоне, послойные якоря H-29c.
>
> **Обновление 2026-08-06 (EXP-13, lr-диагностика — скачок снят).** Контроль полной
> лестницы завершён тем же заходом: первичный профиль «скачок 96→256 + плато»
> разобран триадой lr на 256 ({2.4/2.1/1.7}e-3 → {+5.90/+5.41/+2.88}%, дрейф
> {+2.79/+0.98/+0.62}%) — болезнью оказался **завышенный lr-перенос**, а не
> геометрия зоны. Следствия: lr-таблица уточнена {96:3.0, 256:1.7, 512:1.3}e-3
> (закон ≈(96/d)^0.55); **маркер завышенного lr — дрейф хвоста >1% при включённом
> K2** (самодиагностика переноса без ручной настройки). Честная лестница @2400:
> **+1.66 → +2.88 → +5.64%** (плавная, наклон концов +3.98 п.п. ✓ H-27). Очередь:
> валидация законов на 2048-зоне, H-29c, сборка S0-LM.

**Предел (уточнённый, THEORY §11/FF-12).** Не «локальность невозможна» — точный BP-градиент достижим строго локальным правилом (FF-11, Millidge+ 2022). Предел теперь точный: на цепочной топологии нельзя иметь одновременно (а) дальность сообщений = 1, (б) быструю релаксацию, (в) незатухающий кредит. Точный локальный перенос платит T_rel = O(L²·log 1/ε) (диффузия по цепи); ускорение внутри цепи = затухание α^k кредита. Плюс PC-налог на транспорт весов (FF-13), снимаемый только зонностью (веса зоны в ярусе памяти, p·T_rel ≲ 5).

**Обходы (после цикла 2):** (i) зонная архитектура: мелкая релаксация внутри зон (L_zone = 2–4 ⇒ T_rel ≈ 4–16), межзонный кредит — ранговый скетч по иерархии шин (THEORY §12 Т1/Т4); (ii) мультигрид-иерархия временных шкал для переноса (H-18, гипотеза «rich club = coarse grid»); (iii) событийная релаксация σ-δ поверх остатков (константа и энергия, не асимптотика); (iv) диагональные предобуславливатели (N2 серия 2). Доктрина неизменна: не требовать «zero global», считать *бюджет глобальности* (таблица учёта G — THEORY §11) и минимизировать его.

**Статус:** открыт, но потолок сдвинут: вопрос больше не «существует ли локальное правило BP-качества» (существует, ценой времени), а «какова минимальная цена времени+иерархии на LLM-шкале». Центральный научный вопрос программы остаётся здесь.

---

## C7. Измеримость «мозгоподобия» 🟩

**Потолок методологический:** «приблизиться к мозгу» — не операционализировано. **Обход (принят):** три асимметрии THEORY §0 как проверяемый критерий + энергетическая цена шага ∝ событиям (не размеру) + бюджет глобальности (C6). Любая конструкция, не двигающая ни одну асимметрию, признаётся оптимизацией старого.

---

## C8. Двусмысленность «скорости» 🟩

**Потолок дискурса:** «огромная скорость» распадается на латентность (≤ активные байты/полосу) и обучение-скорость (события×выравнивание, C1). Обе адресованы: первая — битовым бюджетом весов, вторая — рамкой §5. Риск путаницы снят терминологически: в документах фигурирует только «латентность/токен» и «время до уклона γ».

---

## C9. Локальный кредит через петлю времени при неконтрактивной динамике 🟥 (новое, EXP-08)

**Потолок:** EP/PC-обучение рекуррентного носителя требует равновесия энергии по состояниям;
у обучаемого tanh-RNN свободная петля перестаёт быть сжатием (‖Wh‖₂>1 уже при ‖Wh‖_F≈14, d_h=96).
Следствия: (а) релаксация садится на resid-полку 0.02–0.035, непробиваемую за честный T
(предельный цикл EXP-07 через ось времени — предобусловливатель его не вытягивает);
(б) недошедшее равновесие даёт систематическое смещение локальных градиентов: его нельзя
усреднить, гейт может только молчать (EXP-08 S4: 74.5% шагов отброшено, обучение встало на
плате ×2.35 к BP). **Класс:** C6 в новом (временном) измерении. **Обходы (по приоритету):**
(1) контрактивная параметризация Wh (спектральный клип/ортогональность — риск затухания памяти);
(2) линейная или почти-линейная петля состояния (SSM-подобие / каскад шкал §7 — энергия
квадратична, солвер M2 применим по построению); (3) редкий усечённый BP-якорь по расписанию
(доктрина C6, бюджет измеряется); (4) мягкий гейт ∝ качеству релаксации. **Статус:** ⏸ H-25 ✗;
носитель M3 идёт обходной ногой «окно W + амбразура» (EXP-09/H-26).

---

## Повестка дальнейшего думанья (очередь мыслительных задач)

1. **[N1]** Формальная теория блок-итоговой модели оптимума g*: учесть иерархию кэшей (L1/L2/HBM) — g* может оказаться многоуровневой (блок внутри блока по ярусам памяти). **Слита с [N8]** (цикл 2): размер блока и размер зоны — одна переменная.
2. **[N2]** ~~Построить класс энергий равновесного обучения с доказуемо малым бродкастом~~ **Серия 1 закрыта (FF-11..FF-13, THEORY §11); серия 2 частично (цикл 3, EXP-01):** диагональный предобуславливатель проверен численно — ×16–23 константы, но наклон L² топологически не лечится; мультигрид-обход подтверждён (H-18 ✓). **Остаток серии 2:** (а) событийная релаксация остатков (σ-δ поверх солвера — кодек EXP-02 готов, солвер не собран); (б) конструктивный дизайн Π_l с целевым спектральным зазором; (в) перенос всех ✓-результатов на нелинейные широкие блоки.
3. **[N3]** Границы ёмкости рекуррентного состояния и формальный критерий необходимости внешней памяти (C5) — информационный подход через энтропию состояния. **Следующая головная задача после N2.**
4. **[N4]** Динамика эмерджентного выравнивания (DFA): попытаться вывести условия, при которых cos растёт со временем (усреднённая среда, малые шаги) — ответ решит, можно ли закладываться на C1-обход 3 как на базу, а не бонус.
5. **[N5]** Вариационный вывод минимальной частоты событий синхронизации (C3-обход 2): частота сбросов как функция (θ, λ, L). Биопрецедент (тета-циклы) зафиксирован в C3.
6. **[N6]** ~~Формализовать бюджет глобальности~~ **Частично решён (цикл 2):** мера G = бит × границу зон на синапс-апдейт + учётная таблица BP/PC/DFA/скетч (THEORY §11). Остаток: асимптотика G для зонно-иерархического PC (зоны+скелет) в зависимости от дерева шин.
7. **[N7]** (новая, цикл 2) Сколько ярусов у иерархии шин и каковы бюджеты по ярусам: вариационная задача «стоимость бита яруса ↔ вклад яруса в диаметр графа переноса»; ожидаемая форма — экспоненциальное затухание бюджета к корню (аналог 40%-счёта rich club — проверяемый ориентир).
8. **[N8]** (новая, цикл 2; поглощает N1) Оптимальный размер зоны по ярусам памяти: размер, при котором p·T_rel·e_SRAM ≤ 2·e_HBM (условие безналоговости FF-13), и многоуровневое вложение зон SMEM→L2→HBM.

(по мере прохождения переносятся в THEORY.md как новые §, а связанные экспериментальные следствия — в HYPOTHESES.md)
