# Базовая стойка AIra (восстанавливается scripts/setup_env.sh)
numpy<2          # torch 2.1.2 несовместим с numpy 2.x
matplotlib
# torch ставится отдельно в setup_env.sh (тяжёлые CUDA-зависимости; AIRA_NO_TORCH=1 — пропустить)
